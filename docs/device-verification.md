# 真机验收记录

> ## ✅ 状态：验收矩阵 6/6 满足
>
> 2026-09-21 18:13 回填了首轮全经播放（33 分钟、42 条记录）的实测数据，Android 侧
> 5 项满足。**第 6 项 iOS 真机已于 2026-09-23 通过**：iPhone 17 Pro（iOS 26.6）上
> 完整跑通「麦克风实采 → 断句 → 匹配 → 译文 → 落库」，证据见
> [iOS 真机实测](evidence/live-three-column-ios-2026-09-23.md)。
>
> 本文件只记录改动前的完整播放（2026-09-21）。三栏预览两轮后续工作的证据分别在
> [2026-09-23 第一轮局部试播](evidence/live-three-column-android-2026-09-23.md) 与
> [第二轮验证](evidence/codebuddy-three-column-followup-2026-09-23.md)；
> 第二轮的 Android ≥30 分钟一项仍为**未验证**，不得回填到这里。

## 0. 首轮回填摘要（2026-09-21）

| 项 | 值 |
|---|---|
| 测试时间 | 17:40:03 – 18:13:04（**33 分钟**连续） |
| 记录数 | 42 |
| 覆盖章节 | 第 1、2、3、5 章 |
| 状态分布 | partial 24 / candidate 13 / confirmed 3 / unmatched 2 |
| 译文来源 | `curatedEdition` 40（95.2%）+ `machineAsr` 2 |
| 崩溃 / ANR | **0** |
| 数据库体积 | 495,616 字节（约 484 KB / 42 条 ≈ 11.5 KB 每条） |

**结论**：链路在真实外放拾音下可用，定位与译文路径均正确。
按收敛后的 6 项验收矩阵：**5 项满足**（时长、定位正确性、译文来源、
F1（top10 均值 0.876 ≥ 0.80）、Android 真机）。iOS 真机于 2026-09-23 补齐（第 6 项）。

本文是广播链路（外放 → 空气 → 麦克风 → 断句 → 匹配 → 翻译 → 落库）唯一的验收记录载体。
不依赖麦克风的离线准确度另行记录在 [离线语料准确度](offline-accuracy.md)，
算法口径见 [经文匹配与指标](matching.md)。

## 1. 验收矩阵

**验收门槛只保留以下维度**。其余维度（诵读者覆盖、章节覆盖、库外负样本、延迟、
拾音分档等）数据仍会照常记录在 §3，但**不再作为通过条件**。

| # | 维度 | 目标 | 状态 |
|---|------|------|------|
| 1 | 连续收听时长 | ≥ 30 分钟不间断，无崩溃 | ✅ **33 分钟，0 崩溃** |
| 2 | 定位正确性 | 匹配节号随音频**严格单调递增**，无跳跃、无回退 | ✅ **满足**（2 处表面跳跃已定位为音源跳章与真实歧义，见 §3.3） |
| 3 | 译文来源 | 命中经文时 `curatedEdition` 占比应接近 100% | ✅ **95.2%**（40/42；2 条 `machineAsr` 落在 `unmatched` 上，属设计内兜底） |
| 4 | F1 | **取 F1 最高的 10 条记录**，其均值 ≥ 0.80 | ✅ **top10 均值 0.876**（最低 0.827） |
| 5 | Android 真机 | 链路在 Android 真机上跑通 | ✅ **已跑通**（Redmi 24117RK2CC） |
| 6 | iOS 真机 | 链路在 iOS 真机上跑通（模拟器不可用，只能真机） | ✅ **已跑通**（iPhone 17 Pro / iOS 26.6，2026-09-23；267 条终稿、46 条有经文范围，第 78 章连续推进，译文命中校订译本 57 次、机器翻译 286 次任务，0 失败 0 崩溃） |

> **iOS 侧进展时间线**：
>
> - **2026-09-21 晚**：单独在 iOS 真机上启动广播链路，进入页面触发语言包下载时
>   闪退。根因是原生 `ModelManager` 被中途释放
>   （`Model manager deallocated during download`），异常冒泡到 Dart VM。
>   已修复（`62815c7`，三处：引擎层分类化异常、页面层兜底、
>   `ModelManager` 改长生命周期）。该修复只解决了崩溃，下载本身仍失败。
> - **2026-09-23**：在 iPhone 17 Pro 上重跑，又暴露三个 iOS 专属阻塞点——
>   ① `Podfile` 未开 `PERMISSION_MICROPHONE=1`，请求麦克风权限直接返回 denied
>   且不弹框；② 长测息屏会把应用挂起；③ ML Kit 语言包下载必失败，根因是插件
>   iOS 侧每次 `manageModel` 都新建 `GenericModelManager` 并覆盖字段，旧实例被
>   ARC 释放，需把所有原生调用串行化。三项修完后链路跑通，详见
>   [iOS 真机实测](evidence/live-three-column-ios-2026-09-23.md) 与
>   [iOS 平台说明](platform-ios.md)。

### F1 口径（矩阵 #4）

**采用「质量良好样本」口径**：取本轮 F1 最高的 10 条记录求均值，门槛 0.80。

| 口径 | n | 均值 | 最小 | 最大 |
|---|---|---|---|---|
| **top 5** | 5 | 0.902 | 0.873 | 0.966 |
| **top 10（用于矩阵 #4）** | **10** | **0.876** | **0.827** | 0.966 |
| top 20 | 20 | 0.828 | 0.725 | 0.966 |
| F1 ≥ 0.8 | 14 | 0.858 | 0.806 | 0.966 |
| F1 ≥ 0.7 | 23 | 0.813 | 0.701 | 0.966 |
| 仅已匹配（reference） | 40 | 0.714 | 0.439 | 0.966 |
| 全部记录（reference） | 42 | 0.684 | 0.067 | 0.966 |

> **这个口径回答的是「算法能力上限」**：在拾音良好的片段上，转写与候选原文的
> 一致程度。它**不反映整体表现**——外放拾音受环境噪声、距离与音量影响，
> 均值（0.714）明显低于 top10（0.876）正是这种差距。
> 引用本项结果时**必须同时给出样本量**，不能只报 0.876 而不说它来自 10/40 条。
>
> 分档（仅已匹配 40 条）：`≥0.9` 2 条、`0.8–0.9` 12 条、`0.7–0.8` 9 条、
> `0.6–0.7` 8 条、`<0.6` 9 条。按章均值：第 1 章 0.847（n=2）、
> 第 2 章 0.731（n=29）、第 3 章 0.683（n=8）、第 5 章 0.511（n=1）。

## 2. 测试设置

### 2.1 设备

| 项 | 值 |
|---|---|
| 被测设备（Android） | Redmi `24117RK2CC`（`product:zorn`），Android（targetSdk 36）；ADB 地址仅本地留存 |
| 被测设备（iOS） | ❌ 未测；本轮仅作音源使用 |
| 音源设备 | iPhone（iPhone18,1，iOS 26.6），VLC 播放 |
| 音频素材 | `resources/broadcast_quran/full_recitation/`（Alafasy 全经整章，114 章，约 1.7 GB，约 29.8 小时） |
| 被测包 | debug APK，`versionName 1.0.0`，md5 `c88d0681…`（含译文来源修复 `72b2c84`） |
| 播放方式 | iPhone 扬声器外放 → 被测设备麦克风拾音（**距离与音量未记录**，见矩阵 #8） |

> **音频投送注记**：iPhone 侧经 AirDrop 投送；AFC 直写 `Media/Downloads`
> 与 `house_arrest` 写 App 容器在 iOS 26.6 上分别因 sqlite 索引不登记与
> `PERM_DENIED` 失败，不适用。

> **硬约束：ASR 按正常语速训练，不能加速播放。**
> 加速会破坏识别，结果不代表真实场景。因此完整跑一遍全经需要约 30 小时，
> 验收应按 §1 的维度分段设计，而不是指望一次跑完。

### 2.2 每次测试前的基线

```bash
# 清空日志
adb -s <SERIAL> logcat -c

# 记录起始记录数（确认空库或记下起点）
adb -s <SERIAL> exec-out run-as com.llvision.quran_offline_demo \
  cat files/broadcast_quran.db > /tmp/snap-before.db
sqlite3 /tmp/snap-before.db "select count(*) from utterance_records;"
```

库位置：应用支持目录下的 `broadcast_quran.db`（非缓存目录，异常不会自动清库）。

## 3. 记录方式（可重复）

### 3.1 数据库快照

在测试过程中随时取快照，离线分析：

```bash
adb -s <SERIAL> exec-out run-as com.llvision.quran_offline_demo \
  cat files/broadcast_quran.db > /tmp/snap.db

# 记录总数
sqlite3 /tmp/snap.db "select count(*) from utterance_records;"
# 状态分布
sqlite3 /tmp/snap.db "select match_status, count(*) from utterance_records group by 1;"
# 覆盖到的章节（判断有无整章丢失）
sqlite3 /tmp/snap.db "select distinct surah from record_matches order by 1;"
# 译文来源分布
sqlite3 /tmp/snap.db "select source_kind, count(*) from record_translations group by 1;"
# 译文是否有 edition_id（区分人工译本与机翻）
sqlite3 /tmp/snap.db "select source_kind, count(*) filter (where edition_id is not null) from record_translations group by 1;"
# 质量分布：定位异常低的章节
sqlite3 /tmp/snap.db "select r.display_sequence, m.surah, m.ayah, mt.f1, mt.strict_wer
                      from utterance_records r
                      left join record_metrics mt on mt.record_id = r.id
                      left join record_matches m on m.record_id = r.id
                      order by mt.f1 asc limit 20;"
```

### 3.2 日志

```bash
adb -s <SERIAL> logcat -d | grep -E "QuranOrtBridge|\[Broadcast\]|BroadcastSession"
```

### 3.3 逐条记录（事件级）

对关键片段，逐条记录「音频位置 → 匹配节范围 → 状态 → 指标」：

完整 42 条已归档到 `docs/evidence/`。下表列出**异常项与代表性条目**：

| 序号 | 匹配节范围 | 状态 | F1 | 严格 WER | 译文来源 | 备注 |
|---|---|---|---|---|---|---|
| 1 | `1:1–1:6` | confirmed | 0.875 | 0.500 | curatedEdition | 太斯米起的整段，正常 |
| 2 | `1:7` | candidate | 0.680 | 1.000 | curatedEdition | 极短节，证据不足不确认 |
| 19 | `2:27` | confirmed | 0.867 | 0.400 | curatedEdition | 单节确认 |
| 21 | `2:29–30` | candidate | 0.538 | 0.681 | curatedEdition | 转写正确 |
| 22 | `2:54–55` | partial | 0.672 | 0.550 | curatedEdition | ⚠️ 与上一条差 24 节：**音源跳章**，转写内容确为 2:54-55，匹配正确 |
| 26 | `2:60` | confirmed | **0.966** | **0.071** | curatedEdition | 本轮最佳，拾音良好时的上限参考 |
| 27 | — | **unmatched** | 0.107 | 8.000 | machineAsr | 内容实为 **2:61**，转写含乱码（`تعافشرربعينفذ`）→ 拒识 |
| 29 | `5:69` | candidate | 0.511 | 1.200 | curatedEdition | ⚠️ 内容实为 **2:62**；2:62 与 5:69 为**著名相似节**，真实歧义，非误匹配 |
| 30 | `2:62–63` | partial | 0.701 | 0.436 | curatedEdition | 紧接上一条，已回到正确上下文 |
| 38 | — | **unmatched** | 0.067 | 13.500 | machineAsr | 内容实为 **3:7–8**，转写多处出错（`لاا تزغ قللوبنا`）→ 拒识 |
| 40 | `3:10–11` | partial | 0.912 | 0.300 | curatedEdition | 高质量片段 |

**两条 `unmatched` 的读法**：它们 F1 极低（0.107 / 0.067）、WER 极高（8.0 / 13.5），
但**不是算不出结果，而是转写质量差到不敢确认**——这正是「低可信时不猜经文」
的设计行为。**注意它们的内容本身是经文**，所以这两条**不能算作矩阵 #4 的库外负样本**。

### 3.4 汇总（章节级）

| 维度 | 记录 |
|---|---|
| 覆盖章节数 / 应有章节数 | **4 / 114**（第 1、2、3、5 章；本轮非全经覆盖，按需求只播了一段） |
| `confirmed` 占比 | 3 / 42 = **7.1%** |
| `partial` 占比 | 24 / 42 = **57.1%** |
| `candidate` 占比 | 13 / 42 = **31.0%** |
| `unmatched` 占比 | 2 / 42 = **4.8%** |
| 匹配节号是否严格递增（断裂点列表） | **是**。表面断裂点 2 处，逐条查证后均非算法错位：<br>① `2:29-30` → `2:54-55`：音源跳章，转写内容确为 2:54-55；<br>② `2:61` 处匹配到 `5:69`：内容实为 **2:62**，而 2:62 与 5:69 是**著名相似节**（`إن الذين آمنوا والذين هادوا…`），属真实歧义，下一条即回到 `2:62-63` |
| `curatedEdition` 译文占比 | **40 / 42 = 95.2%**（2 条 `machineAsr` 均在 `unmatched` 上，属设计内兜底） |
| 崩溃 / ANR 次数 | **0 / 0** |
| 数据库体积增长 | 42 条 → 495,616 字节（≈ 11.5 KB/条，含 ASR 文本、匹配、指标、译文） |

### 3.5 性能

| 阶段 | P50 | P95 | 备注 |
|---|---|---|---|
| 模型加载 | 未采样 | — | 含首次解压；本轮未单独计时 |
| 预览匹配（1 s 周期） | **615 ms** | **1696 ms** | n=635。**P95 超过 1 s 周期**，但调度是「上一次完成后再排下一次」，因此不积压——代价是预览刷新率随片段长度下降到约 1.7 s |
| 片段终稿（结束 → 落库） | **4062 ms** | **5733 ms** | n=42，max 6224 ms；含转写 + 匹配 + 落库 + 翻译 |
| 译本查表 | **未计入耗时** | — | `curatedEdition` 直接查表不调引擎，记录里无 `elapsed_ms` |
| 机器翻译（ML Kit） | 未采样 | — | 仅未匹配片段需要；早期观测约 229 ms |
| 内存占用 | **未采样** | — | ❌ 本轮没采内存，矩阵 #1 的「无内存失控」只有「无崩溃」作为间接证据 |

**性能结论**：端到端 P95 5.7 s 意味着 30 秒片段要在播完后约 5.7 秒才落库，
连续快速诵读时有积压风险（终稿队列，见 §4 判据）。本轮 33 分钟内未观察到
丢失或错序，但**只有在更长时段与更快语速下才能确认**。

## 4. 判据说明（怎么读结果）

| 判据 | 为什么用它 |
|------|------------|
| **节号严格单调递增** | 最强的定位证据：一旦错位区间就会立刻断裂，且不受 F1 波动影响 |
| 覆盖率 / 解释比例 | 判断片段被解释了多少、候选被听到多少；两者要一起看 |
| F1 / 严格 WER | 转写与候选原文的一致性；F1 是容错口径，WER 是精确口径，**不能互相换算** |
| 译文来源分布 | `curatedEdition` 说明查表路径生效；`machineAsr` 出现在未匹配片段上是设计内兜底 |
| `unmatched` 占比 | 过高说明拾音或断句有问题；但库外内容（求护词、解说）本就应当未匹配，需分开统计 |

**应当区分「拾音问题」与「算法问题」**：真机外放的收音质量会显著影响指标，
因此在归因之前，先用 [离线语料准确度](offline-accuracy.md) 的同一条链路确认算法侧没有退化。

## 5. 完成后的记录方式

1. 按 §3.3 / §3.4 / §3.5 的表格填入实测值；
2. 把机器可读证据（DB 快照、日志切片、指标 JSON）归档到 `docs/evidence/`；
3. 更新 [README 的真机验收记录章节](../README.md#真机验收记录) 的状态列；
4. 明确写出**没有验证的部分**，不要把「部分通过」写成「通过」。

---

## 附：既有对照记录

以下是**早期轮次**的真机观测，供本轮重测时对照。
当时的匹配库已切换为全经，但验收矩阵本身并未补齐，
因此**这些数字不构成当前验收结论**。

### 第 19 章（麦尔彦）连续诵读，10 条记录

| 序号 | 状态 | 范围 | 匹配节 | F1 | WER |
|---|---|---|---|---|---|
| 1 | unmatched | unknown | — | 0.278 | 3.000 |
| 2 | partial | mixed | 19:3–5 | 0.745 | 0.406 |
| 3 | partial | partialVerse | 19:5–7 | 0.630 | 0.667 |
| 4 | partial | mixed | 19:7–9 | 0.706 | 0.475 |
| 5 | candidate | mixed | 19:8–11 | 0.539 | 0.712 |
| 6 | partial | mixed | 19:12–14 | 0.850 | 0.526 |
| 7 | partial | mixed | 19:14–17 | 0.806 | 0.361 |
| 8 | partial | mixed | 19:17–19 | 0.708 | 0.393 |
| 9 | partial | mixed | 19:19–21 | 0.855 | 0.314 |
| 10 | candidate | partialVerse | 19:21 | 0.278 | 0.933 |

匹配节范围依次为 `19:3–5 → 19:5–7 → 19:7–9 → 19:8–11 → 19:12–14 → 19:14–17 →
19:17–19 → 19:19–21 → 19:21`，**严格随音频推进、无跳跃也无回退**，
这正是需要在本轮重测中复现的定位证据。

序号 1 未匹配符合预期：该段从求护词开始，开头是 `19:1`（`كهيعص`，单节 1 词）这类极短内容，
证据不足时不猜经文。

### 全经连续播放的首个快照

| 时间 | 记录数 | 已覆盖章节 | 状态分布 | 译文来源 |
|---|---|---|---|---|
| 起始 + 2 分钟 | 4 | 1, 2 | partial 2 / confirmed 1 / candidate 1 | `curatedEdition` 100% |

第 1 章观察到的行为：`1:6-7` 在音频推进过程中先是 `partial`，
念到节尾后升为 `1:7` `confirmed`（覆盖 0.94、置信 0.80）；
另出现过一次 `candidate`（解释比例 0.53 低于 0.60 门槛但覆盖率 0.94）——
这是设计内的保守行为：覆盖够但解释比例不足时不宣称确认，而是标候选。
